/*Operadores de comparação
1. > maior que
2. >= maior que ou igual 
3. < menor que
4. <= menor que ou igual
5.  == igualdade : checa valor (=) atribuição
6. === igualdade restrita : checa valor e tipo 
7. != diferença entre valores
8. !== diferença restrita  cehca valor e tipo
*/

const num1 = 10;
const num2 = 10;

const comp = num1 !== num2;

console.log(comp)
